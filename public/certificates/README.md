# Certificates

Place certificate files here, named to match `fileUrl` in
`data/certifications.ts`:

- nptel-java.pdf
- rotaract-community-service.pdf

Supports PDF or image files — set `fileType: "image"` in the data
file for image certificates (e.g. .png / .jpg).
