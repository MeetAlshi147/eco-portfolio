# Activity files

Place uploaded activity PDFs here, named to match `fileUrl` in
`data/activities.ts`:

- assignment-0.pdf
- crossword.pdf
- pledge.pdf

Until a file is added, the Activities section automatically shows a
"File coming soon" placeholder instead of a broken preview.
